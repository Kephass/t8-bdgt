# macOS
.DS_Store

# Editor / IDE cruft
*.swp
*.swo
.idea/
.vscode/

# Claude Code local overrides (per-machine; not for sharing)
.claude/settings.local.json

# Python preview server cruft
__pycache__/
*.pyc

# NOTE: config.js IS committed. It holds only the Supabase URL + publishable
# key — both designed to ship in client code. RLS in the database protects
# data, not the key itself. If you ever swap in an `sb_secret_…` key,
# gitignore that file instead.
